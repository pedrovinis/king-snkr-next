import { nav_bar } from './layout/nav-bar'
import { footer } from './layout/footer'
import { index } from './pages/index'
import { profile } from './pages/profile'
import { config } from './pages/config'
import { buttons } from './buttons'


const messages = {
    en: {
        translations: {
            nav_bar,
            footer,
            buttons,
            index,
            profile,
            config
        }
    }
}

export { messages }