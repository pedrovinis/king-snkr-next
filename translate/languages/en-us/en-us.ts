import { nav_bar } from './layout/nav-bar'
import { profile } from './pages/profile'
import { config } from './pages/config'


const messages = {
    en: {
        translations: {
            nav_bar,
            profile,
            config
        }
    }
}

export { messages }